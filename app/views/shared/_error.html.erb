 <% if obj.errors.any?  %>
  <div class="row">
    <div class="col-md-8 col-md-offset-2">
      <div class="error">
      
        <h2>
          <%= pluralize(obj.errors.count, "error") %> 
          prevented this from getting saved.
        </h2>
        
        <ul>
        <% obj.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      
  
      </div>
    </div>
  </div>
<% end %>